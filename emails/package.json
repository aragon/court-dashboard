{
  "name": "emails",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "handlebars": "^4.7.3",
    "prettier": "^1.19.1"
  },
  "scripts": {
    "build": "rm -rf ./dist && ./generate-templates && cp -r assets ./dist/assets",
    "build:mock": "MOCK_DATA=1 yarn build"
  }
}

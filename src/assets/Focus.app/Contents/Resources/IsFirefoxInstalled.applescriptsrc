on main()
    try
        tell application "Finder"
            if name of application file id "org.mozilla.firefox" is not equal to "" then
                return true
            end if
        end tell
    on error err_msg number err_num
        return false
    end try
end main

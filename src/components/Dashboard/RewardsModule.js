import React from 'react'
import { Box } from '@aragon/ui'

const RewardsModule = React.memo(function RewardsModule({ rewards }) {
  return <Box heading="Last rewards">Rewards</Box>
})

export default RewardsModule
